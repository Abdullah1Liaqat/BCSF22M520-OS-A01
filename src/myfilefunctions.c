#include "../include/myfilefunctions.h"
#include <string.h>
#include <stdlib.h>

int wordCount(FILE* file, int* lines, int* words, int* chars) { return 0; }
int mygrep(FILE* fp, const char* search_str, char*** matches) { return 0; }
